version: '3.8'

services:
  riscv-assembler:
    build: .
    container_name: riscv_dev
    working_dir: /app
    volumes:
      - .:/app
    stdin_open: true
    tty: true
    environment:
      - PYTHONDONTWRITEBYTECODE=1